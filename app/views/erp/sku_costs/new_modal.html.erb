<%= turbo_frame_tag "erp_modal" do %>
  <div class="erp-modal-backdrop" data-controller="modal" data-action="click->modal#closeOnBackdrop">
    <section class="erp-modal erp-modal--wide" role="dialog" aria-modal="true" aria-labelledby="sku-cost-modal-title">
      <div class="erp-modal__header">
        <h2 id="sku-cost-modal-title"><%= t("erp.sku_costs.actions.add_cost") %></h2>
        <button class="icon-button" type="button" aria-label="<%= t("common.actions.close") %>" data-action="modal#close">
          <i class="bi bi-x-lg" aria-hidden="true"></i>
        </button>
      </div>
      <%= render "form", cost: @cost, sku_options: @sku_options, modal: true %>
    </section>
  </div>
<% end %>
